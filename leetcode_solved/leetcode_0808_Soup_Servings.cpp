/**
 * 思路：喝汤问题。
 * 起始有等量的 N ml 的 A,B 两种汤，定义了四种舀汤的操作，每次随机执行一种舀汤操作的一次操作。
 * 如果某一次舀汤时不能按预期数量提供，那么尽可能多的接近要求提供汤。
 * 当两种汤都舀空时，操作终止。
 * 输出是 P(A 汤先被要舀空) + P(A,B 汤同时被舀空) / 2
 *
 */
class Solution {
public:
    double soupServings(int N) {
        
    }
};
