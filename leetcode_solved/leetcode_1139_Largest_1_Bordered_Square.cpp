class Solution {
public:
	/**
	 * 注意，这里不是找子正方形使其所有元素都为 1， 而是 border，边上都为1
	 *
	 */
    int largest1BorderedSquare(vector<vector<int>>& grid) {
        int row = grid.size();
        int col = grid[0].size();

        
    }
};
